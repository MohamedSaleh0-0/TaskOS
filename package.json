{
  "name": "tasks-os",
  "version": "1.0.0",
  "description": "Tasks-OS Plugin for Obsidian",
  "main": "dist/main.js",
  "scripts": {
    "build": "esbuild src/main.ts --bundle --external:obsidian --outfile=dist/main.js --platform=node --target=es2022"
  },
  "dependencies": {},
  "devDependencies": {
    "esbuild": "^0.21.0",
    "obsidian": "^1.5.7",
    "typescript": "^5.0.0"
  }
}